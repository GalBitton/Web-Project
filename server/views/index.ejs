<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>API Status</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        header {
            background-color: #3498db;
            color: #fff;
            padding: 1rem;
            text-align: center;
        }

        main {
            background-color: #fff;
            padding: 2rem;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin: 1rem;
            text-align: center;
        }

        footer {
            margin-top: 2rem;
            color: #555;
            font-size: 0.8rem;
        }
    </style>
</head>

<body>
<header>
    <h1>Server Status</h1>
</header>

<main>
    <p>Server is active and running smoothly.</p>
</main>

<footer id="footer"></footer>
<script>
    import container from "../containerConfig";
    const serviceName = container.get("serviceName");
    const footer = document.getElementById("footer");
    footer.innerHTML = `<p>{serviceName} © ${new Date().getFullYear()}</p>`;
</script>
</body>

</html>